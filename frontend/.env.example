# =============================================================================
# PencoLab Frontend — variables para Vercel / local
# Copia este archivo a .env (local) o configúralas en el panel de Vercel.
# =============================================================================

# URL pública del backend en Render (sin barra final).
# Ejemplo: https://pencolab-api.onrender.com
# En local puedes dejarla vacía: Vite usa el proxy /api → localhost:8000
VITE_API_URL=https://TU-SERVICIO.onrender.com
